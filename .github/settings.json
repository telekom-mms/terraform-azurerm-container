{
  "repos": {
    "description": "A Terraform module that manages the container resources from the azurerm provider.",
    "visibility": "public",
    "default_branch": "main",
    "topics": [
      "azure",
      "container-registry",
      "kubernetes-cluster",
      "terraform"
    ]
  }
}
